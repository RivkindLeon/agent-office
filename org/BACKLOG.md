---
kind: backlog
owner: founder
---

# Долги компании

Сюда попадает всё, что заметили и не починили сразу. Правило простое: **увидел
проблему, не чинишь сейчас — запиши здесь**. Иначе через неделю её не вспомнит
никто, а компания второй раз наступит на то же самое.

Это не список задач для сотрудников: у них работа приходит триггерами. Это
память основателя о том, где система дырявая.

| # | Долг | Чем грозит | Где увидели |
|---|---|---|---|
| 1 | Найм в компании не создаёт агента в OpenClaw, и ничто это не сверяет | Должность числится нанятой, а смена падает с `Unknown agent id`. Проверить из репозитория нельзя: конфиг рантайма живёт вне его | шесть подряд упавших смен продакта, 2026-09-03 |
| 2 | Триггер `record-hire` у кадровика мёртв | Оргструктура генерируется, найм фиксируется событием основателя — сработать триггер не может | вердикт `head-of-people 11.0`, существо |
| 3 | Шаг `submit` в сборке пакета не исполняется | Как только появляется восьмой файл, состояние уходит в `review_pending` и объявленный шаг становится недостижим. Результат верный, но план и движок расходятся | сборка пакета продакта, шаг 9 |
| 4 | Никто не замечает «есть работа, которую некому делать» | Повод №1 для найма из хартии не реализован. Заявку на инженера подал основатель вручную. Частично закрыто пунктом INBOX `no_doer`, но только после приёмки брифа | обсуждение цепочки, 2026-09-03 |
| 5 | Событие `handoff` никогда не создаётся | Схема его требует, валидатор проверяет, «Офис» должен рисовать им передачу работы — но ни одна инструкция не говорит «передал — запиши» | схема против инструкций |
| 6 | Лимит расхода — декларация | `budget_tokens` никто не считает и не обрывает; агенты не пишут `usage` в события. Метрики «энергия» и «цена артефакта» пока невозможны | `PROFILE.md` против журнала |
| ~~7~~ | ~~Шаги брифа отмечает сам агент~~ | **Закрыт 2026-09-03:** готовность шага выводится из полей артефакта | — |
| 8 | Второй проход обязателен для всех шагов | На коротких и структурных артефактах даёт ноль находок: устав в три строки, JSON-манифест. Лишний вызов модели | находки по шагам 1,0,1,0,1,0,1,1 |
| 9 | Каждая смена перечитывает весь корпус | Главный расход времени и квоты. Нужна выжимка состояния вместо чтения всех пакетов целиком | час семнадцать на восемь смен |
| 10 | Проверка копипасты ловит структурные строки | Три совпадения в `COMMS.md` — заголовок таблицы. При пороге 3 однажды свалит работу на пустом месте | приёмка пакета продакта |
| 11 | Нет формата вердикта на рабочий результат | Для брифа решение выражается статусом. Появится вторая продуктовая должность — понадобится вердикт | `org/WORK.md` |
| 12 | Мак и сервер расходятся | Git живёт только на сервере, синхронизация по rsync дважды затирала сгенерированные файлы | затёртый `org/ORG.md`, дважды |
| 13 | Бюджет продакта 150 000 не измерен | Число никто не назначал, утверждено временно. Пересмотреть после первой настоящей работы | вердикт `head-of-product 1.0`, форма |
| 14 | Кадровик читает пакеты всех должностей целиком | При двух-пяти должностях нормально, при тридцати — O(N) контекста на каждое кадровое действие | внешний разбор, 2026-09-02 |
| 15 | Метрик нет | Доля принятых с первого круга, возвраты по версии, цена артефакта — данные в журнале копятся, считалки нет | `docs/VALIDATION.md`, раздел «что меряем» |
| 17 | Парсер front matter читает только плоские ключи | Агент написал вложенный блок `users:` — движок его молча не увидел. Либо учить парсер, либо проверять форму front matter | бриф калькулятора, 2026-09-03 |
| ~~18~~ | ~~Шесть невалидных событий в журнале~~ | **Ложный, закрыт 2026-09-05:** внешний аудит проверил — валидатор показывает ноль битых. Починили и забыли обновить запись; реестр дыр сам не проверялся | аудит |
| 20 | У кадровика нет обязанности следить за рынком моделей | Раз в неделю проверять, какие модели доступны аккаунту, и сообщать об изменениях — как HR следит за рынком труда. Сегодня о пропаже модели узнаём по упавшей смене. Нужен недельный триггер и доступ к каталогу моделей | предложение основателя, 2026-09-03 |
| ~~21~~ | ~~Шаг `submit` неисполним, у проекта нет конечного состояния~~ **Закрыт 2026-09-05:** сдача через `DELIVERY.md`, приёмка основателем, триггер не повторяется | Инженер закончит все задачи — триггер продолжит срабатывать вечно, а основателю никто не скажет, что работа готова. Нужен статус `engineering-done` в артефакте инженера и пункт INBOX «принять результат» | аудит, п. 1.8 |
| 22 | Счётчик кругов не считается | `round` — свободное число в front matter; у продакта все вердикты `round: 1`, значит «третий круг к основателю» не наступит никогда. Считать круги в редьюсере по числу возвратов | аудит, п. 1.10 |
| 23 | `void: true` нигде не описан | Недокументированный ластик истории возвратов, а вся метрика качества строится на них | аудит, п. 1.9 |
| 24 | Заметки читает одна должность из трёх | У продакта и инженера `notes/` есть в инструкции на запись и отсутствуют во входе. Память, в которую пишут и не читают | аудит, п. 2.1 |
| ~~25~~ | ~~Инженер пишет в `INBOX.md`, который перезаписывается~~ **Закрыт 2026-09-05:** канал сдачи — свой артефакт проекта | Его канал передачи стирается той же сменой | аудит, п. 2.4 |
| 26 | «Шесть документов» против семи, версия в `PROFILE.md` против манифеста, `PROGRESS.md` устарел целиком | Документы расходятся с кодом в пяти местах; сотрудник, следующий инструкции буквально, провалит приёмку | аудит, пп. 2.8, 2.10, 5.1 |
| 27 | Нет должности, владеющей движком | `org/tools/**` правит только основатель, а обязанность улучшать проверки записана кадровику. Обязанность без прав | аудит, п. 4.1 |
| 28 | Нет петли от метрик к правке пакета | Возвраты копятся, заявку на пересмотр никто не оформляет. «Описание не работает» не входит в четыре повода для найма | аудит, п. 4.2 |
| 29 | Нет отката к прошлой версии пакета | Если 11.0 хуже 10.0, вернуться нечем | аудит, п. 4.4 |
| 30 | Нет очереди и понятия «работа взята» | Два сотрудника с пересекающимися правами на `projects/**` не заметят друг друга; длину очереди, названную поводом №1 для найма, измерить нечем | аудит, пп. 2.6, 4.5 |
| 31 | Долгий прогон, привязанный к ssh, умирает молча | Цикл смен, запущенный через ssh, гибнет вместе с сессией: полтора часа выглядели как работа, а сделано было ноль. Вывод через `tail` показывался только в конце, поэтому тишина не отличалась от занятости. Запускать отвязанно (`nohup`), писать прогресс в файл на сервере построчно | 2026-09-05, прогон T-005 |
| 16 | Компания заморожена вручную | Слот `agent-office-shift-daily` снят с расписания. Пока смены запускаются руками — решить, когда возвращать | 2026-09-03 |

## Чего здесь нет намеренно

Прототип «Офиса» вне репозитория и три вердикта с пометкой `legacy` — это не
долги, а решения. Первое — чтобы продукт строили с нуля по требованиям, второе —
честная история о том, что правило появилось позже.
