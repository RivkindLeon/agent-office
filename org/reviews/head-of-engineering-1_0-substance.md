---
kind: review
role: head-of-engineering
package_version: "1.0"
round: 1
dimension: substance
verdict: changes_requested
analysis_by: assistant
read_by: founder
decided_by: founder
---

# Ревью: head-of-engineering 1.0, существо

Вердикт: **вернуть**

Проверял основатель как `hiring_manager` заявки.

## По существу сделано хорошо

- цепочка замкнута без подсказки: триггер ждёт бриф, принятый основателем;
- задачный режим перенесён в манифест точно: `design → tasks → task → submit`
  со `steps_from` на `projects/{target}/tasks.json`;
- критерии приёмки закрепляют паттерн компании — красная и зелёная фазы в
  разные смены, вывод прогона обязателен, заявления исполнителя недостаточно
  (`roles/head-of-engineering/ACCEPTANCE.md`);
- в инструкции найден законный путь вокруг запрета править чужой артефакт:
  запрос на смену статуса брифа кладётся в `org/INBOX.md`.

## Блокеры

- [ ] **Триггер прибит к одному проекту.** `front_matter_equals` на
      `projects/calculator/BRIEF.md` плюс `target: "calculator"`. Должность
      отвечает за инженерную работу компании, а не за калькулятор: на втором
      проекте она не проснётся. В движке есть общая операция `project_status`,
      ей пользуется `roles/head-of-product/manifest.json` — взять её со
      статусом `ready-for-engineering`, `target` убрать.

- [ ] **Модель выбрана не под работу.** В манифесте `openai/gpt-5.6-luna` —
      это модель кадровой и продуктовой работы. Для кода на этой машине
      используется Codex-вариант `openai/gpt-5.6-sol`: на нём работают ночные
      кодинг-джобы. Решение основателя: **primary `openai/gpt-5.6-sol`,
      фолбэк `openai/gpt-5.6-luna`**.

## Замечания (работу не возвращают)

- Грейд `senior` и бюджет 150 000 должность назначила себе сама. Основатель их
  **утверждает**, но в `PROFILE.md` это должно быть помечено как решение
  основателя, а не как собственный вывод — у продуктовой должности такая
  пометка есть.
- Две границы из пяти повторяют общие запреты хартии («не выбирает бюджет»,
  «не нанимает людей»). Пункт 12 общих критериев: граница должна называть
  соседнюю работу.

## Отклонённые возражения

Нет.
